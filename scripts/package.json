{
  "name": "akash-console-proto-scripts",
  "version": "0.1.0",
  "description": "Generate proto definitions from Akash Console API Zod schemas",
  "type": "module",
  "scripts": {
    "generate": "tsx src/generate-proto.ts",
    "schemas": "tsx src/console-api-schemas.ts"
  },
  "dependencies": {
    "@globalart/zod-to-proto": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
