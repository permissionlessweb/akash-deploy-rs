# Auto-generated from console.deployment — do not edit.
# Source: terp-rs/proto/src/gen/console.deployment.rs
# Package: console.deployment
# Run `just py-gen` to regenerate.
from __future__ import annotations

import json
from dataclasses import dataclass, field, asdict
from typing import Any, List, Optional

PACKAGE = "console.deployment"

@dataclass
class ListDeploymentsRequest:
    skip: float = 0.0
    limit: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentDeploymentId:
    owner: str = ""
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentDeploymentId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentDeploymentId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentDeployment:
    id: Optional[ListDeploymentsResponseDataDeploymentDeploymentId] = None
    state: str = ""
    hash: str = ""
    created_at: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentDeployment", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentDeployment":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLeaseId:
    owner: str = ""
    dseq: str = ""
    gseq: float = 0.0
    oseq: float = 0.0
    provider: str = ""
    bseq: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLeaseId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLeaseId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLeasePrice:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLeasePrice", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLeasePrice":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLeaseStatusForwardedPort:
    port: float = 0.0
    external_port: float = 0.0
    host: str = ""
    available: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLeaseStatusForwarded_port", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLeaseStatusForwardedPort":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLeaseStatusIp:
    ip: str = ""
    port: float = 0.0
    external_port: float = 0.0
    protocol: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLeaseStatusIp", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLeaseStatusIp":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLeaseStatusService:
    name: str = ""
    available: float = 0.0
    total: float = 0.0
    uris: List[str] = field(default_factory=list)
    observed_generation: float = 0.0
    replicas: float = 0.0
    updated_replicas: float = 0.0
    ready_replicas: float = 0.0
    available_replicas: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLeaseStatusService", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLeaseStatusService":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLeaseStatus:
    forwarded_ports: Optional[Any  # Any] = None
    ips: Optional[Any  # Any] = None
    services: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLeaseStatus", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLeaseStatus":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentLease:
    id: Optional[ListDeploymentsResponseDataDeploymentLeaseId] = None
    state: str = ""
    price: Optional[ListDeploymentsResponseDataDeploymentLeasePrice] = None
    created_at: str = ""
    closed_on: str = ""
    reason: str = ""
    status: Optional[ListDeploymentsResponseDataDeploymentLeaseStatus] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentLease", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentLease":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccountId:
    scope: str = ""
    xid: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentEscrow_accountId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentEscrowAccountId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccountStateTransferred:
    denom: str = ""
    amount: str = ""

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccountStateFund:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentEscrow_accountStateFund", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentEscrowAccountStateFund":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccountStateDepositBalance:
    denom: str = ""
    amount: str = ""

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccountStateDeposit:
    owner: str = ""
    height: str = ""
    source: str = ""
    balance: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentEscrow_accountStateDeposit", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentEscrowAccountStateDeposit":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccountState:
    owner: str = ""
    state: str = ""
    transferred: List[Any] = field(default_factory=list)
    settled_at: str = ""
    funds: List[Any] = field(default_factory=list)
    deposits: List[Any] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentEscrow_accountState", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentEscrowAccountState":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeploymentEscrowAccount:
    id: Optional[ListDeploymentsResponseDataDeploymentEscrowAccountId] = None
    state: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeploymentEscrow_account", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeploymentEscrowAccount":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataDeployment:
    deployment: Optional[Any  # Any] = None
    leases: List[ListDeploymentsResponseDataDeploymentLease] = field(default_factory=list)
    escrow_account: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataDeployment", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataDeployment":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseDataPagination:
    total: float = 0.0
    skip: float = 0.0
    limit: float = 0.0
    has_more: bool = False
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseDataPagination", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseDataPagination":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponseData:
    deployments: List[ListDeploymentsResponseDataDeployment] = field(default_factory=list)
    pagination: Optional[ListDeploymentsResponseDataPagination] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListDeploymentsResponse:
    data: Optional[ListDeploymentsResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.ListDeploymentsResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListDeploymentsResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentRequest:
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataDeploymentId:
    owner: str = ""
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataDeploymentId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataDeploymentId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataDeployment:
    id: Optional[GetDeploymentResponseDataDeploymentId] = None
    state: str = ""
    hash: str = ""
    created_at: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataDeployment", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataDeployment":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLeaseId:
    owner: str = ""
    dseq: str = ""
    gseq: float = 0.0
    oseq: float = 0.0
    provider: str = ""
    bseq: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLeaseId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLeaseId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLeasePrice:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLeasePrice", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLeasePrice":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLeaseStatusForwardedPort:
    port: float = 0.0
    external_port: float = 0.0
    host: str = ""
    available: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLeaseStatusForwarded_port", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLeaseStatusForwardedPort":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLeaseStatusIp:
    ip: str = ""
    port: float = 0.0
    external_port: float = 0.0
    protocol: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLeaseStatusIp", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLeaseStatusIp":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLeaseStatusService:
    name: str = ""
    available: float = 0.0
    total: float = 0.0
    uris: List[str] = field(default_factory=list)
    observed_generation: float = 0.0
    replicas: float = 0.0
    updated_replicas: float = 0.0
    ready_replicas: float = 0.0
    available_replicas: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLeaseStatusService", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLeaseStatusService":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLeaseStatus:
    forwarded_ports: Optional[Any  # Any] = None
    ips: Optional[Any  # Any] = None
    services: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLeaseStatus", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLeaseStatus":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataLease:
    id: Optional[GetDeploymentResponseDataLeaseId] = None
    state: str = ""
    price: Optional[GetDeploymentResponseDataLeasePrice] = None
    created_at: str = ""
    closed_on: str = ""
    reason: str = ""
    status: Optional[GetDeploymentResponseDataLeaseStatus] = None
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataLease", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataLease":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccountId:
    scope: str = ""
    xid: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_accountId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccountId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccountStateTransferred:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_accountStateTransferred", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccountStateTransferred":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccountStateFund:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_accountStateFund", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccountStateFund":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccountStateDepositBalance:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_accountStateDepositBalance", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccountStateDepositBalance":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccountStateDeposit:
    owner: str = ""
    height: str = ""
    source: str = ""
    balance: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_accountStateDeposit", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccountStateDeposit":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccountState:
    owner: str = ""
    state: str = ""
    transferred: List[Any] = field(default_factory=list)
    settled_at: str = ""
    funds: List[GetDeploymentResponseDataEscrowAccountStateFund] = field(default_factory=list)
    deposits: List[Any] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_accountState", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccountState":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseDataEscrowAccount:
    id: Optional[GetDeploymentResponseDataEscrowAccountId] = None
    state: Optional[GetDeploymentResponseDataEscrowAccountState] = None
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseDataEscrow_account", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseDataEscrowAccount":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponseData:
    deployment: Optional[GetDeploymentResponseDataDeployment] = None
    leases: List[GetDeploymentResponseDataLease] = field(default_factory=list)
    escrow_account: Optional[GetDeploymentResponseDataEscrowAccount] = None
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentResponse:
    data: Optional[GetDeploymentResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentByOwnerDseqRequest:
    owner: str = ""
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentByOwnerDseqRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentByOwnerDseqRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentByOwnerDseqResponseLeaseProviderAttribute:
    key: str = ""
    value: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentByOwnerDseqResponseLeaseProviderAttribute", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentByOwnerDseqResponseLeaseProviderAttribute":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentByOwnerDseqResponseLeaseProvider:
    address: str = ""
    host_uri: str = ""
    is_deleted: bool = False
    attributes: List[Any] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentByOwnerDseqResponseLeaseProvider", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentByOwnerDseqResponseLeaseProvider":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentByOwnerDseqResponseLease:
    gseq: float = 0.0
    oseq: float = 0.0
    provider: Optional[GetDeploymentByOwnerDseqResponseLeaseProvider] = None
    status: str = ""
    monthly_cost_u_denom: float = 0.0
    cpu_units: float = 0.0
    gpu_units: float = 0.0
    memory_quantity: float = 0.0
    storage_quantity: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentByOwnerDseqResponseLease", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentByOwnerDseqResponseLease":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentByOwnerDseqResponseEvent:
    tx_hash: str = ""
    date: str = ""
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentByOwnerDseqResponseEvent", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentByOwnerDseqResponseEvent":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetDeploymentByOwnerDseqResponse:
    owner: str = ""
    dseq: str = ""
    balance: float = 0.0
    denom: str = ""
    status: str = ""
    total_monthly_cost_u_denom: float = 0.0
    leases: List[GetDeploymentByOwnerDseqResponseLease] = field(default_factory=list)
    events: List[GetDeploymentByOwnerDseqResponseEvent] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.GetDeploymentByOwnerDseqResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetDeploymentByOwnerDseqResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CreateDeploymentRequestData:
    sdl: str = ""
    deposit: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.CreateDeploymentRequestData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CreateDeploymentRequestData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CreateDeploymentRequest:
    data: Optional[CreateDeploymentRequestData] = None
    TYPE_URL: str = field(default="/console.deployment.CreateDeploymentRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CreateDeploymentRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CreateDeploymentResponseDataSignTx:
    data: str = ""
    TYPE_URL: str = field(default="/console.deployment.CreateDeploymentResponseDataSignTx", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CreateDeploymentResponseDataSignTx":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CreateDeploymentResponseData:
    dseq: str = ""
    manifest: str = ""
    sign_tx: Optional[CreateDeploymentResponseDataSignTx] = None
    TYPE_URL: str = field(default="/console.deployment.CreateDeploymentResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CreateDeploymentResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CreateDeploymentResponse:
    data: Optional[CreateDeploymentResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.CreateDeploymentResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CreateDeploymentResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentRequestDataCertificate:
    cert_pem: str = ""
    key_pem: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentRequestDataCertificate", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentRequestDataCertificate":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentRequestData:
    sdl: str = ""
    certificate: Optional[UpdateDeploymentRequestDataCertificate] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentRequestData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentRequestData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentRequest:
    data: Optional[UpdateDeploymentRequestData] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataDeploymentId:
    owner: str = ""
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataDeploymentId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataDeploymentId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataDeployment:
    id: Optional[UpdateDeploymentResponseDataDeploymentId] = None
    state: str = ""
    hash: str = ""
    created_at: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataDeployment", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataDeployment":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLeaseId:
    owner: str = ""
    dseq: str = ""
    gseq: float = 0.0
    oseq: float = 0.0
    provider: str = ""
    bseq: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLeaseId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLeaseId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLeasePrice:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLeasePrice", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLeasePrice":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLeaseStatusForwardedPort:
    port: float = 0.0
    external_port: float = 0.0
    host: str = ""
    available: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLeaseStatusForwarded_port", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLeaseStatusForwardedPort":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLeaseStatusIp:
    ip: str = ""
    port: float = 0.0
    external_port: float = 0.0
    protocol: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLeaseStatusIp", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLeaseStatusIp":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLeaseStatusService:
    name: str = ""
    available: float = 0.0
    total: float = 0.0
    uris: List[str] = field(default_factory=list)
    observed_generation: float = 0.0
    replicas: float = 0.0
    updated_replicas: float = 0.0
    ready_replicas: float = 0.0
    available_replicas: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLeaseStatusService", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLeaseStatusService":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLeaseStatus:
    forwarded_ports: Optional[Any  # Any] = None
    ips: Optional[Any  # Any] = None
    services: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLeaseStatus", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLeaseStatus":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataLease:
    id: Optional[UpdateDeploymentResponseDataLeaseId] = None
    state: str = ""
    price: Optional[UpdateDeploymentResponseDataLeasePrice] = None
    created_at: str = ""
    closed_on: str = ""
    reason: str = ""
    status: Optional[UpdateDeploymentResponseDataLeaseStatus] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataLease", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataLease":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccountId:
    scope: str = ""
    xid: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_accountId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccountId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccountStateTransferred:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_accountStateTransferred", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccountStateTransferred":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccountStateFund:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_accountStateFund", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccountStateFund":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccountStateDepositBalance:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_accountStateDepositBalance", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccountStateDepositBalance":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccountStateDeposit:
    owner: str = ""
    height: str = ""
    source: str = ""
    balance: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_accountStateDeposit", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccountStateDeposit":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccountState:
    owner: str = ""
    state: str = ""
    transferred: List[Any] = field(default_factory=list)
    settled_at: str = ""
    funds: List[Any] = field(default_factory=list)
    deposits: List[Any] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_accountState", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccountState":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseDataEscrowAccount:
    id: Optional[UpdateDeploymentResponseDataEscrowAccountId] = None
    state: Optional[UpdateDeploymentResponseDataEscrowAccountState] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseDataEscrow_account", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseDataEscrowAccount":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponseData:
    deployment: Optional[UpdateDeploymentResponseDataDeployment] = None
    leases: List[UpdateDeploymentResponseDataLease] = field(default_factory=list)
    escrow_account: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class UpdateDeploymentResponse:
    data: Optional[UpdateDeploymentResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.UpdateDeploymentResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "UpdateDeploymentResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CloseDeploymentRequest:
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.CloseDeploymentRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CloseDeploymentRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CloseDeploymentResponseData:
    success: bool = False
    TYPE_URL: str = field(default="/console.deployment.CloseDeploymentResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CloseDeploymentResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class CloseDeploymentResponse:
    data: Optional[CloseDeploymentResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.CloseDeploymentResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "CloseDeploymentResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentRequestData:
    dseq: str = ""
    deposit: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentRequestData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentRequestData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentRequest:
    data: Optional[DepositDeploymentRequestData] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataDeploymentId:
    owner: str = ""
    dseq: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataDeploymentId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataDeploymentId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataDeployment:
    id: Optional[DepositDeploymentResponseDataDeploymentId] = None
    state: str = ""
    hash: str = ""
    created_at: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataDeployment", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataDeployment":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLeaseId:
    owner: str = ""
    dseq: str = ""
    gseq: float = 0.0
    oseq: float = 0.0
    provider: str = ""
    bseq: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLeaseId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLeaseId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLeasePrice:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLeasePrice", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLeasePrice":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLeaseStatusForwardedPort:
    port: float = 0.0
    external_port: float = 0.0
    host: str = ""
    available: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLeaseStatusForwarded_port", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLeaseStatusForwardedPort":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLeaseStatusIp:
    ip: str = ""
    port: float = 0.0
    external_port: float = 0.0
    protocol: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLeaseStatusIp", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLeaseStatusIp":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLeaseStatusService:
    name: str = ""
    available: float = 0.0
    total: float = 0.0
    uris: List[str] = field(default_factory=list)
    observed_generation: float = 0.0
    replicas: float = 0.0
    updated_replicas: float = 0.0
    ready_replicas: float = 0.0
    available_replicas: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLeaseStatusService", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLeaseStatusService":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLeaseStatus:
    forwarded_ports: Optional[Any  # Any] = None
    ips: Optional[Any  # Any] = None
    services: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLeaseStatus", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLeaseStatus":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataLease:
    id: Optional[DepositDeploymentResponseDataLeaseId] = None
    state: str = ""
    price: Optional[DepositDeploymentResponseDataLeasePrice] = None
    created_at: str = ""
    closed_on: str = ""
    reason: str = ""
    status: Optional[DepositDeploymentResponseDataLeaseStatus] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataLease", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataLease":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccountId:
    scope: str = ""
    xid: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_accountId", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccountId":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccountStateTransferred:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_accountStateTransferred", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccountStateTransferred":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccountStateFund:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_accountStateFund", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccountStateFund":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccountStateDepositBalance:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_accountStateDepositBalance", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccountStateDepositBalance":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccountStateDeposit:
    owner: str = ""
    height: str = ""
    source: str = ""
    balance: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_accountStateDeposit", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccountStateDeposit":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccountState:
    owner: str = ""
    state: str = ""
    transferred: List[Any] = field(default_factory=list)
    settled_at: str = ""
    funds: List[Any] = field(default_factory=list)
    deposits: List[Any] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_accountState", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccountState":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseDataEscrowAccount:
    id: Optional[DepositDeploymentResponseDataEscrowAccountId] = None
    state: Optional[DepositDeploymentResponseDataEscrowAccountState] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseDataEscrow_account", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseDataEscrowAccount":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponseData:
    deployment: Optional[DepositDeploymentResponseDataDeployment] = None
    leases: List[DepositDeploymentResponseDataLease] = field(default_factory=list)
    escrow_account: Optional[Any  # Any] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class DepositDeploymentResponse:
    data: Optional[DepositDeploymentResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.DepositDeploymentResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "DepositDeploymentResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListWithResourcesRequest:
    address: str = ""
    skip: float = 0.0
    limit: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.ListWithResourcesRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListWithResourcesRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListWithResourcesResponseResultLeaseProvider:
    address: str = ""
    host_uri: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListWithResourcesResponseResultLeaseProvider", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListWithResourcesResponseResultLeaseProvider":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListWithResourcesResponseResultLeasePrice:
    denom: str = ""
    amount: str = ""
    TYPE_URL: str = field(default="/console.deployment.ListWithResourcesResponseResultLeasePrice", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListWithResourcesResponseResultLeasePrice":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListWithResourcesResponseResultLease:
    id: str = ""
    owner: str = ""
    provider: Optional[ListWithResourcesResponseResultLeaseProvider] = None
    dseq: str = ""
    gseq: float = 0.0
    oseq: float = 0.0
    state: str = ""
    price: Optional[ListWithResourcesResponseResultLeasePrice] = None
    TYPE_URL: str = field(default="/console.deployment.ListWithResourcesResponseResultLease", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListWithResourcesResponseResultLease":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListWithResourcesResponseResult:
    owner: str = ""
    dseq: str = ""
    status: str = ""
    created_height: float = 0.0
    cpu_units: float = 0.0
    gpu_units: float = 0.0
    memory_quantity: float = 0.0
    storage_quantity: float = 0.0
    leases: List[ListWithResourcesResponseResultLease] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.ListWithResourcesResponseResult", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListWithResourcesResponseResult":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class ListWithResourcesResponse:
    count: float = 0.0
    results: List[ListWithResourcesResponseResult] = field(default_factory=list)
    TYPE_URL: str = field(default="/console.deployment.ListWithResourcesResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "ListWithResourcesResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetWeeklyDeploymentCostRequest:
    TYPE_URL: str = field(default="/console.deployment.GetWeeklyDeploymentCostRequest", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetWeeklyDeploymentCostRequest":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetWeeklyDeploymentCostResponseData:
    weekly_cost: float = 0.0
    TYPE_URL: str = field(default="/console.deployment.GetWeeklyDeploymentCostResponseData", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetWeeklyDeploymentCostResponseData":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

@dataclass
class GetWeeklyDeploymentCostResponse:
    data: Optional[GetWeeklyDeploymentCostResponseData] = None
    TYPE_URL: str = field(default="/console.deployment.GetWeeklyDeploymentCostResponse", init=False, repr=False)

    def encode(self) -> bytes:
        """Encode to protobuf binary (requires native extension)."""
        from akash_deploy._native import encode_message  # type: ignore[import]
        return encode_message(self.TYPE_URL, json.dumps(asdict(self)).encode())

    @classmethod
    def decode(cls, data: bytes) -> "GetWeeklyDeploymentCostResponse":
        """Decode from protobuf binary (requires native extension)."""
        from akash_deploy._native import decode_message  # type: ignore[import]
        return cls(**json.loads(decode_message(cls.TYPE_URL, data)))

    def to_dict(self) -> dict:
        return asdict(self)

    def to_json(self) -> str:
        return json.dumps(asdict(self))

