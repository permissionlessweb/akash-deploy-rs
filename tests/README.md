# Test Suite

This directory contains a complete integration test suite that validates the canonicity of various formatting and workflows this crate provides

## Quick Start

```bash
# Run integration test (Rust generates → Go verifies)
just test
```
